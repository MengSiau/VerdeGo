// Used as a route to render the VehicleForm screen (handles both add and edit).
// IMPORTANT -> filename here acts as the route path

import { VehicleForm } from '@/src/screens/MyVehicles/VehicleForm';

export default function VehicleFormRoute() {
  return <VehicleForm />;
}
