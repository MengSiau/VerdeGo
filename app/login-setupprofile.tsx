// Used as a route to render the LoginSetUpProfile screen.
// IMPORTANT -> filename here acts as the route path

import { LoginSetUpProfile } from '@/src/screens/LoginSetUpProfile/LoginSetUpProfile';

export default function LoginSetUpProfileRoute() {
  return <LoginSetUpProfile />;
}
