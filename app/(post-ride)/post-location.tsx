// Used as a route to render the PostLocation screen.
// IMPORTANT -> filename here acts as the route path

import { PostLocation } from '@/src/screens/PostRide/PostLocation/PostLocation';

export default function PostLocationRoute() {
  return <PostLocation />;
}
